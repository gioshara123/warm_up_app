import 'package:flutter/material.dart';

class WarmUpPage extends StatelessWidget {
  static const String ID = "./WarmUpPage";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello World!'),
      ),
    );
  }
}
